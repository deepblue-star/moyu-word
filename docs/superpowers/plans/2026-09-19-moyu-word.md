# 摸鱼单词 Implementation Plan

> **For agentic workers:** Use subagent-driven-development to implement and review the bounded tasks below.

**Goal:** Deliver and install a small offline x86 Windows vocabulary and PDF reader matching the requested hover/opacity behavior.

**Architecture:** WPF on Windows 10's built-in .NET Framework; x86 executable and bundled x86 PDFium. Transparent window with separate background and foreground layers, local JSON storage, no network code in the application. Work in the fresh clone on `develop`; no existing code or baseline tests are present.

**Tech Stack:** C# 5, .NET Framework, WPF, PDFium, PowerShell build, C# per-user installer.

## Components and tasks

- [x] `src/Models.cs`, `src/LibraryStore.cs`, `assets/tem4.json`: downloaded traceable TEM-4 library; JSON/CSV import, validation, deduplication and atomic settings/favorites; 10 storage tests pass.
- [x] `src/PdfDocument.cs`, `native/pdfium.dll`, `third-party/`: pinned x86 PDFium, provenance/licenses, Unicode file paths, bounded transparent rendering; 23 native PDF checks pass.
- [x] `src/MainWindow.cs`, `src/Theme.cs`, `src/Program.cs`: compact complete UI; 24 UI/placement and 14 asynchronous PDF checks pass; installed UI exercised directly.
- [x] `src/WindowBehavior.cs`: independent alpha, native hover polling/click-through, smooth hide/show, resize/drag/title controls, pin, tray/recovery. Verified real hide/reentry over Notepad, transparency, drag/resize, minimize/maximize and close.
- [x] `scripts/build.ps1`, `installer/Setup.cs`: portable ZIP + offline per-user setup installed successfully; 33 installed payload hashes match; x86 process confirmed. Seven installer and four build-publication checks pass.
- [x] `tests/`, `docs/verification.md`, `README.md`: evidence and limitations documented, no runtime network APIs/endpoints, attribution retained, independent review findings resolved; source committed on develop.

## Acceptance evidence

Record commands and runtime observations in `docs/verification.md`. Required evidence: x86 EXE + DLL; no external runtime download; actual TEM-4 source/count; valid import survives restart; favorites and settings survive restart; hidden answer resets on navigation; mouse swipe and buttons; independent alpha transparency; leave/reenter; pin plus title controls and resize; real multipage PDF/zoom; installed app launch; no application networking; build artifacts with checksums. Preserve all requested scope and do not mark completed on unit tests alone.
